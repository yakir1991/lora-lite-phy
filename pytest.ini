[pytest]
# Only run our new, non-legacy tests by default
addopts = -q
# Exclude legacy tests and heavy compatibility suite unless explicitly requested
norecursedirs = legacy build external receiver __pycache__
# We can still run specific tests by path if needed
